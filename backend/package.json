{
  "name": "hexviewer-backend",
  "version": "1.0.0",
  "description": "Serves file listing + raw byte ranges for the hex viewer, without ever loading a whole file into memory.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}